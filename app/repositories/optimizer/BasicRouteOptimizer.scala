package repositories.optimizer

//class BasicRouteOptimizer @Inject() (distanceRepository: DistanceRepository) extends RouteOptimizer {
//    override def optimize(orders: List[DeliveryOrderModel]): List[DeliveryOrderModel] = {
//        val locations = orders.map(_.)
//    }
//}
